import {css} from 'styled-components'

const textColor = '#0055FB'
const textSize = '16px'
const cursorType = 'pointer'

const styles = css`
    color: ${textColor};
    font-size: ${textSize};
    cursor: ${cursorType};
`

export default styles